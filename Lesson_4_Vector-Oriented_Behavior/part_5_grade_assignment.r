scores <- c(82, 91, 76, 45, 67)
grades <- ifelse(scores >= 75, "Pass", "Fail")

R
scores <- c(82, 91, 76, 45, 67)
grades <- ifelse(scores >= 75, "Pass", "Fail")


[1] "Pass" "Pass" "Pass" "Fail" "Fail"

